# adofai-battle — copy to `.env.local`
#
# Public OAuth clients omit client_secret — register redirect on IdP, e.g.:
#   {NEXT_PUBLIC_APP_URL}/api/auth/oauth/callback
#
# SQLite file path defaults in-app; omit DATABASE_URL unless you extend schemas.
